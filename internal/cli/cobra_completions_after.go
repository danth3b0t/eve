package cli

// Candidate providers now live in cobra_completions.go. This split file is
// intentionally retained so historical imports/tests resolve to the unified
// implementation rather than reviving a separate protocol path.
